let a = 1;
let b = 0;

// if (a > 0) {
//     b = a;
// } else {
//     b += 1; // add +1
// }

// выражение, ? если true, если false;

// a > 0 ?  a : b += 1;
// console.log(`b: ${b}`);

let color = 'yellow';

switch (color) {
    case 'yellow':
        console.log('Color is yellow');
        break;
    case 'red':
        console.log('color is red');
        break;
    default:
        console.log('Default');
}